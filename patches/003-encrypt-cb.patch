--- a/proxinstall
+++ b/proxinstall
@@ -1358,6 +1358,16 @@ my $create_raid_disk_grid = sub {
 my $create_raid_advanced_grid = sub {
     my ($hdsize_btn) = @_;
     my $labeled_widgets = [];
+
+    my $encrypt_checkbox = Gtk3::CheckButton->new();
+    $encrypt_checkbox->signal_connect(
+        "toggled" => sub {
+            my $encrypt_cb = shift;
+            Proxmox::Install::Config::set_encrypt(!!$encrypt_cb->get_active());
+        },
+    );
+    push @$labeled_widgets, ['encrypt', $encrypt_checkbox];
+
     my $spinbutton_ashift = Gtk3::SpinButton->new_with_range(9, 13, 1);
     $spinbutton_ashift->set_tooltip_text(
         "zpool ashift property (pool sector size, default 2^12)");
@@ -1447,6 +1457,15 @@ my $create_btrfs_raid_advanced_grid = sub {
     my ($hdsize_btn) = @_;
     my $labeled_widgets = [];
 
+    my $encrypt_checkbox = Gtk3::CheckButton->new();
+    $encrypt_checkbox->signal_connect(
+        "toggled" => sub {
+            my $encrypt_cb = shift;
+            Proxmox::Install::Config::set_encrypt(!!$encrypt_cb->get_active());
+        },
+    );
+    push @$labeled_widgets, ['encrypt', $encrypt_checkbox];
+
     my $combo_compress = Gtk3::ComboBoxText->new();
     $combo_compress->set_tooltip_text("btrfs compression algorithm for boot volume");
     my $comp_opts = ["on", "off", "zlib", "lzo", "zstd"];
@@ -1533,6 +1552,15 @@ sub create_hdoption_view {
 
     my $hdsize_labeled_widgets = [];
 
+    my $encrypt_checkbox = Gtk3::CheckButton->new();
+    $encrypt_checkbox->signal_connect(
+        "toggled" => sub {
+            my $encrypt_cb = shift;
+            Proxmox::Install::Config::set_encrypt(!!$encrypt_cb->get_active());
+        },
+    );
+    push @$hdsize_labeled_widgets, ['encrypt', $encrypt_checkbox, ''];
+
     my $target_hd = Proxmox::Install::Config::get_target_hd();
     my $hdsize = 0; # size compute
     if (-b $target_hd) {
